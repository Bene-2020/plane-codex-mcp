# 项目开发约束

本文件适用于整个仓库。所有实现都必须遵守以下规则。

## 保持代码直接

- 使用满足当前 Demo 规格的最简单实现，不写过度防御性代码。
- 只处理规格已经定义或测试实际复现的失败，不为假想场景增加分支。
- 不增加层层包装、重复校验、catch-all、静默吞错、多套兜底、自动降级或推测性兼容代码。
- 数据只在系统边界校验一次；进入可信内部模块后不要重复校验同一份数据。
- 没有两个真实实现需要共享时，不提前抽象接口、工厂、策略、适配层或通用框架。
- 错误应清楚暴露。仅在规格明确要求的 Hook 和异步 Plane 同步边界采用 fail-open 或重试。

## 禁止哈希式标识

- 不使用 SHA 或其他加密哈希生成 ID、幂等键、去重键、缓存键或字段所有权标记。
- 项目事件批次直接使用可读的组合唯一键：`project_context_id + session_id + turn_id`。
- 数据库主键使用自增整数。批次 ID 使用 `batch_<rowid>`，事件 ID 使用 `event_<batch_rowid>_<index>`；不要额外引入 UUID/ULID。字段比较保存规范化原值或版本信息，不保存哈希摘要。

## 不设置开发 Gate

- 不编写 go/no-go gate、阶段准入 gate、功能 gate、审批 gate 或 capability gate。
- 测试和最小闭环用于发现问题并指导下一步，不得实现为阻止后续开发的代码关卡。
- 不为未来可能出现的宿主能力差异建立 feature flag 或自动 capability detection。
- 除规格明确要求的“自动捕获开关”外，不新增功能开关。
- Plane 只实现当前目标实例实际使用的 API 路径；不要预先构建 `/issues/`、`/work-items/` 的自动探测和 fallback 链。

## 测试原则

- 优先测试用户可观察行为和关键数据流，不为实现细节堆积测试。
- 每个已知关键故障保留一个直接测试即可，不做重复的防御性测试矩阵。
- 发现真实兼容问题后再添加最小修复和对应回归测试。
