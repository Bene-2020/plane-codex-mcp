"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Links = void 0;
const BaseResource_1 = require("./BaseResource");
/**
 * Links API resource
 * Handles all link related operations for work items
 */
class Links extends BaseResource_1.BaseResource {
    constructor(config) {
        super(config);
    }
    /**
     * Create a new link for a work item
     */
    async create(workspaceSlug, projectId, issueId, createLink) {
        return this.post(`/workspaces/${workspaceSlug}/projects/${projectId}/work-items/${issueId}/links/`, createLink);
    }
    /**
     * Retrieve a link by ID
     */
    async retrieve(workspaceSlug, projectId, issueId, linkId) {
        return this.get(`/workspaces/${workspaceSlug}/projects/${projectId}/work-items/${issueId}/links/${linkId}/`);
    }
    /**
     * Update a link
     */
    async update(workspaceSlug, projectId, issueId, linkId, updateLink) {
        return this.patch(`/workspaces/${workspaceSlug}/projects/${projectId}/work-items/${issueId}/links/${linkId}/`, updateLink);
    }
    /**
     * Delete a link
     */
    async delete(workspaceSlug, projectId, issueId, linkId) {
        return this.httpDelete(`/workspaces/${workspaceSlug}/projects/${projectId}/work-items/${issueId}/links/${linkId}/`);
    }
    /**
     * List links for a work item with optional filtering
     */
    async list(workspaceSlug, projectId, issueId, params) {
        return this.get(`/workspaces/${workspaceSlug}/projects/${projectId}/work-items/${issueId}/links/`, params);
    }
}
exports.Links = Links;
//# sourceMappingURL=Links.js.map