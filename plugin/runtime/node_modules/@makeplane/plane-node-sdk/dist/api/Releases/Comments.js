"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Comments = void 0;
const BaseResource_1 = require("../BaseResource");
/**
 * ReleaseComments sub-resource
 * Manages comments on a release
 */
class Comments extends BaseResource_1.BaseResource {
    constructor(config) {
        super(config);
    }
    /**
     * List comments on a release
     */
    async list(workspaceSlug, releaseId, params) {
        const data = await this.get(`/workspaces/${workspaceSlug}/releases/${releaseId}/comments/`, params);
        return Array.isArray(data) ? data : data.results;
    }
    /**
     * Retrieve a single release comment
     */
    async retrieve(workspaceSlug, releaseId, commentId) {
        return this.get(`/workspaces/${workspaceSlug}/releases/${releaseId}/comments/${commentId}/`);
    }
    /**
     * Create a comment on a release
     */
    async create(workspaceSlug, releaseId, data) {
        return this.post(`/workspaces/${workspaceSlug}/releases/${releaseId}/comments/`, data);
    }
    /**
     * Update a release comment
     */
    async update(workspaceSlug, releaseId, commentId, data) {
        return this.patch(`/workspaces/${workspaceSlug}/releases/${releaseId}/comments/${commentId}/`, data);
    }
    /**
     * Delete a release comment
     */
    async delete(workspaceSlug, releaseId, commentId) {
        return this.httpDelete(`/workspaces/${workspaceSlug}/releases/${releaseId}/comments/${commentId}/`);
    }
}
exports.Comments = Comments;
//# sourceMappingURL=Comments.js.map