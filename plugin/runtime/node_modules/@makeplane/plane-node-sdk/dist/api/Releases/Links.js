"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Links = void 0;
const BaseResource_1 = require("../BaseResource");
/**
 * ReleaseLinks sub-resource
 * Manages external links attached to a release
 */
class Links extends BaseResource_1.BaseResource {
    constructor(config) {
        super(config);
    }
    /**
     * List links on a release
     */
    async list(workspaceSlug, releaseId, params) {
        const data = await this.get(`/workspaces/${workspaceSlug}/releases/${releaseId}/links/`, params);
        return Array.isArray(data) ? data : data.results;
    }
    /**
     * Retrieve a single release link
     */
    async retrieve(workspaceSlug, releaseId, linkId) {
        return this.get(`/workspaces/${workspaceSlug}/releases/${releaseId}/links/${linkId}/`);
    }
    /**
     * Create a link on a release
     */
    async create(workspaceSlug, releaseId, data) {
        return this.post(`/workspaces/${workspaceSlug}/releases/${releaseId}/links/`, data);
    }
    /**
     * Update a release link
     */
    async update(workspaceSlug, releaseId, linkId, data) {
        return this.patch(`/workspaces/${workspaceSlug}/releases/${releaseId}/links/${linkId}/`, data);
    }
    /**
     * Delete a release link
     */
    async delete(workspaceSlug, releaseId, linkId) {
        return this.httpDelete(`/workspaces/${workspaceSlug}/releases/${releaseId}/links/${linkId}/`);
    }
}
exports.Links = Links;
//# sourceMappingURL=Links.js.map