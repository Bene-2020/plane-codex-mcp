"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Estimates = void 0;
const BaseResource_1 = require("./BaseResource");
/**
 * Estimates API resource
 * Manages project estimates and estimate points
 */
class Estimates extends BaseResource_1.BaseResource {
    constructor(config) {
        super(config);
    }
    // ===== ESTIMATE CRUD =====
    /**
     * Create a new estimate for a project
     */
    async create(workspaceSlug, projectId, createEstimate) {
        return this.post(`/workspaces/${workspaceSlug}/projects/${projectId}/estimates/`, createEstimate);
    }
    /**
     * Retrieve the estimate configured for a project
     */
    async retrieve(workspaceSlug, projectId) {
        return this.get(`/workspaces/${workspaceSlug}/projects/${projectId}/estimates/`);
    }
    /**
     * Update the estimate for a project
     */
    async update(workspaceSlug, projectId, updateEstimate) {
        return this.patch(`/workspaces/${workspaceSlug}/projects/${projectId}/estimates/`, updateEstimate);
    }
    /**
     * Delete the estimate for a project
     */
    async delete(workspaceSlug, projectId) {
        return this.httpDelete(`/workspaces/${workspaceSlug}/projects/${projectId}/estimates/`);
    }
    /**
     * Link an estimate to a project so it becomes the active estimate system.
     * Delegates to the project update endpoint (PATCH .../projects/{projectId}).
     */
    async linkToProject(workspaceSlug, projectId, estimateId) {
        return this.patch(`/workspaces/${workspaceSlug}/projects/${projectId}/`, { estimate: estimateId });
    }
    // ===== ESTIMATE POINTS =====
    /**
     * List all estimate points for a project estimate
     */
    async listPoints(workspaceSlug, projectId, estimateId) {
        return this.get(`/workspaces/${workspaceSlug}/projects/${projectId}/estimates/${estimateId}/estimate-points/`);
    }
    /**
     * Create estimate points for a project estimate.
     * The API accepts a JSON array directly as the request body.
     */
    async createPoints(workspaceSlug, projectId, estimateId, points) {
        return this.post(`/workspaces/${workspaceSlug}/projects/${projectId}/estimates/${estimateId}/estimate-points/`, points);
    }
    /**
     * Update a single estimate point
     */
    async updatePoint(workspaceSlug, projectId, estimateId, estimatePointId, updatePoint) {
        return this.patch(`/workspaces/${workspaceSlug}/projects/${projectId}/estimates/${estimateId}/estimate-points/${estimatePointId}/`, updatePoint);
    }
    /**
     * Delete a single estimate point
     */
    async deletePoint(workspaceSlug, projectId, estimateId, estimatePointId) {
        return this.httpDelete(`/workspaces/${workspaceSlug}/projects/${projectId}/estimates/${estimateId}/estimate-points/${estimatePointId}/`);
    }
}
exports.Estimates = Estimates;
//# sourceMappingURL=Estimates.js.map