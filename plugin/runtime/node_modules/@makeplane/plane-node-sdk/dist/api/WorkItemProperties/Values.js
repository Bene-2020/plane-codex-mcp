"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Values = void 0;
const BaseResource_1 = require("../BaseResource");
/**
 * WorkItemPropertyValues API resource
 * Handles all work item property value operations
 */
class Values extends BaseResource_1.BaseResource {
    constructor(config) {
        super(config);
    }
    /**
     * Retrieve a property value by ID
     */
    async retrieve(workspaceSlug, projectId, workItemId, propertyId) {
        const propertyValues = await this.get(`/workspaces/${workspaceSlug}/projects/${projectId}/work-items/${workItemId}/work-item-properties/${propertyId}/values/`);
        return propertyValues;
    }
    /**
     * List property values for a work item
     */
    async list(workspaceSlug, projectId, workItemId, params) {
        return this.get(`/workspaces/${workspaceSlug}/projects/${projectId}/work-items/${workItemId}/work-item-properties/values/`, params);
    }
    /**
     * Create/update a property value
     *
     * For single-value properties:
     * - Acts as an upsert operation (create or update)
     * - Returns a single WorkItemPropertyValues
     *
     * For multi-value properties (is_multi=True):
     * - Replaces all existing values with the new ones (sync operation)
     * - Returns a list of values
     */
    async create(workspaceSlug, projectId, workItemId, propertyId, updateData) {
        return this.post(`/workspaces/${workspaceSlug}/projects/${projectId}/work-items/${workItemId}/work-item-properties/${propertyId}/values/`, updateData);
    }
    /**
     * Update an existing property value(s) (partial update)
     *
     * For single-value properties:
     * - Updates the existing value
     * - Returns a single WorkItemPropertyValues
     *
     * For multi-value properties (is_multi=True):
     * - Replaces all existing values with the new ones (sync operation)
     * - Returns a list of values
     *
     * @throws {HttpError} If the property value does not exist (404)
     */
    async update(workspaceSlug, projectId, workItemId, propertyId, updateData) {
        return this.patch(`/workspaces/${workspaceSlug}/projects/${projectId}/work-items/${workItemId}/work-item-properties/${propertyId}/values/`, updateData);
    }
    /**
     * Delete the property value(s) for a work item
     *
     * For single-value properties:
     * - Deletes the single value
     *
     * For multi-value properties (is_multi=True):
     * - Deletes all values for that property
     *
     * @throws {HttpError} If the property value does not exist (404)
     */
    async delete(workspaceSlug, projectId, workItemId, propertyId) {
        return this.httpDelete(`/workspaces/${workspaceSlug}/projects/${projectId}/work-items/${workItemId}/work-item-properties/${propertyId}/values/`);
    }
}
exports.Values = Values;
//# sourceMappingURL=Values.js.map