"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Milestones = void 0;
const BaseResource_1 = require("./BaseResource");
/**
 * Milestones API resource
 * Handles all milestone related operations
 */
class Milestones extends BaseResource_1.BaseResource {
    constructor(config) {
        super(config);
    }
    /**
     * Create a new milestone
     */
    async create(workspaceSlug, projectId, createMilestone) {
        return this.post(`/workspaces/${workspaceSlug}/projects/${projectId}/milestones/`, createMilestone);
    }
    /**
     * Retrieve a milestone by ID
     */
    async retrieve(workspaceSlug, projectId, milestoneId) {
        return this.get(`/workspaces/${workspaceSlug}/projects/${projectId}/milestones/${milestoneId}/`);
    }
    /**
     * Update a milestone
     */
    async update(workspaceSlug, projectId, milestoneId, updateMilestone) {
        return this.patch(`/workspaces/${workspaceSlug}/projects/${projectId}/milestones/${milestoneId}/`, updateMilestone);
    }
    /**
     * Delete a milestone
     */
    async delete(workspaceSlug, projectId, milestoneId) {
        return this.httpDelete(`/workspaces/${workspaceSlug}/projects/${projectId}/milestones/${milestoneId}/`);
    }
    /**
     * List milestones with optional filtering
     */
    async list(workspaceSlug, projectId, params) {
        return this.get(`/workspaces/${workspaceSlug}/projects/${projectId}/milestones/`, params);
    }
    /**
     * Add work items to a milestone
     */
    async addWorkItems(workspaceSlug, projectId, milestoneId, issueIds) {
        return this.post(`/workspaces/${workspaceSlug}/projects/${projectId}/milestones/${milestoneId}/work-items/`, {
            issues: issueIds,
        });
    }
    /**
     * Remove work items from a milestone
     */
    async removeWorkItems(workspaceSlug, projectId, milestoneId, issueIds) {
        return this.httpDelete(`/workspaces/${workspaceSlug}/projects/${projectId}/milestones/${milestoneId}/work-items/`, {
            issues: issueIds,
        });
    }
    /**
     * List work items in a milestone
     */
    async listWorkItems(workspaceSlug, projectId, milestoneId, params) {
        return this.get(`/workspaces/${workspaceSlug}/projects/${projectId}/milestones/${milestoneId}/work-items/`, params);
    }
}
exports.Milestones = Milestones;
//# sourceMappingURL=Milestones.js.map