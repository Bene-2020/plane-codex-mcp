"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Members = void 0;
const BaseResource_1 = require("../BaseResource");
/**
 * CollectionMembers sub-resource
 * Manages members of a (typically private) collection
 */
class Members extends BaseResource_1.BaseResource {
    constructor(config) {
        super(config);
    }
    /**
     * List members of a collection
     */
    async list(workspaceSlug, collectionId) {
        const data = await this.get(`/workspaces/${workspaceSlug}/collections/${collectionId}/members/`);
        return Array.isArray(data) ? data : data.results;
    }
    /**
     * Add a member to a collection
     */
    async add(workspaceSlug, collectionId, memberData) {
        return this.post(`/workspaces/${workspaceSlug}/collections/${collectionId}/members/`, memberData);
    }
    /**
     * Update a collection member's access level.
     *
     * @param memberId - UUID of the CollectionMember row (not the user id)
     */
    async update(workspaceSlug, collectionId, memberId, memberData) {
        return this.patch(`/workspaces/${workspaceSlug}/collections/${collectionId}/members/${memberId}/`, memberData);
    }
    /**
     * Remove a member from a collection.
     *
     * @param memberId - UUID of the CollectionMember row (not the user id)
     */
    async remove(workspaceSlug, collectionId, memberId) {
        return this.httpDelete(`/workspaces/${workspaceSlug}/collections/${collectionId}/members/${memberId}/`);
    }
}
exports.Members = Members;
//# sourceMappingURL=Members.js.map