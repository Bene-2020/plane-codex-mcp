"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Cycles = void 0;
const BaseResource_1 = require("./BaseResource");
const WorkItems_1 = require("./WorkItems");
/**
 * Cycles API resource
 * Handles all cycle (sprint) related operations
 */
class Cycles extends BaseResource_1.BaseResource {
    constructor(config) {
        super(config);
    }
    /**
     * Create a new cycle
     */
    async create(workspaceSlug, projectId, createCycle) {
        return this.post(`/workspaces/${workspaceSlug}/projects/${projectId}/cycles/`, createCycle);
    }
    /**
     * Retrieve a cycle by ID
     */
    async retrieve(workspaceSlug, projectId, cycleId) {
        return this.get(`/workspaces/${workspaceSlug}/projects/${projectId}/cycles/${cycleId}/`);
    }
    /**
     * Update a cycle
     */
    async update(workspaceSlug, projectId, cycleId, updateCycle) {
        return this.patch(`/workspaces/${workspaceSlug}/projects/${projectId}/cycles/${cycleId}/`, updateCycle);
    }
    /**
     * Delete a cycle
     */
    async delete(workspaceSlug, projectId, cycleId) {
        return this.httpDelete(`/workspaces/${workspaceSlug}/projects/${projectId}/cycles/${cycleId}/`);
    }
    /**
     * List cycles with optional filtering
     */
    async list(workspaceSlug, projectId) {
        return this.get(`/workspaces/${workspaceSlug}/projects/${projectId}/cycles/`);
    }
    /**
     * List cycles as a paginated "lite" response, intended for pickers and
     * reference lookups. Accepts an optional status filter.
     */
    async listLite(workspaceSlug, projectId, params) {
        return this.get(`/workspaces/${workspaceSlug}/projects/${projectId}/cycles-lite/`, params);
    }
    /**
     * List archived cycles
     */
    async listArchived(workspaceSlug, projectId, params) {
        return this.get(`/workspaces/${workspaceSlug}/projects/${projectId}/archived-cycles/`, params);
    }
    /**
     * Unarchive a cycle
     */
    async unArchive(workspaceSlug, projectId, cycleId) {
        return this.httpDelete(`/workspaces/${workspaceSlug}/projects/${projectId}/archived-cycles/${cycleId}/unarchive/`);
    }
    /**
     * Archive a cycle
     */
    async archive(workspaceSlug, projectId, cycleId) {
        return this.post(`/workspaces/${workspaceSlug}/projects/${projectId}/cycles/${cycleId}/archive/`);
    }
    /**
     * List work items in a cycle.
     *
     * Supports the same `filters` and `pql` query parameters as
     * {@link WorkItems.list}.
     */
    async listWorkItemsInCycle(workspaceSlug, projectId, cycleId, params) {
        return this.get(`/workspaces/${workspaceSlug}/projects/${projectId}/cycles/${cycleId}/cycle-issues/`, (0, WorkItems_1.prepareWorkItemParams)(params));
    }
    /**
     * Add work items to cycle
     */
    async addWorkItemsToCycle(workspaceSlug, projectId, cycleId, workItemIds) {
        return this.post(`/workspaces/${workspaceSlug}/projects/${projectId}/cycles/${cycleId}/cycle-issues/`, {
            issues: workItemIds,
        });
    }
    /**
     * Remove work item from cycle
     */
    async removeWorkItemFromCycle(workspaceSlug, projectId, cycleId, workItemId) {
        return this.httpDelete(`/workspaces/${workspaceSlug}/projects/${projectId}/cycles/${cycleId}/cycle-issues/${workItemId}/`);
    }
    /**
     * Transfer work items to another cycle
     */
    async transferWorkItemsToAnotherCycle(workspaceSlug, projectId, cycleId, transferData) {
        return this.post(`/workspaces/${workspaceSlug}/projects/${projectId}/cycles/${cycleId}/transfer-issues/`, transferData);
    }
}
exports.Cycles = Cycles;
//# sourceMappingURL=Cycles.js.map