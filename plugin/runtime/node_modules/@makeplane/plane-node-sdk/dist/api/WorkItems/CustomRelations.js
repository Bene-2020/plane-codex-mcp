"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CustomRelations = void 0;
const BaseResource_1 = require("../BaseResource");
/**
 * WorkItemCustomRelations API resource
 * Manages custom (definition-based) work item relations.
 *
 * Custom relations are workspace-level types defined via the
 * work-item-relation-definitions endpoint. Each definition has an outward
 * label and an inward label that controls directionality.
 */
class CustomRelations extends BaseResource_1.BaseResource {
    constructor(config) {
        super(config);
    }
    /**
     * List all custom relations for a work item grouped by definition label.
     * Response keys are the outward/inward labels from active workspace relation
     * definitions (e.g. "implements", "implemented by", "relates to").
     */
    async list(workspaceSlug, projectId, workItemId) {
        return this.get(`/workspaces/${workspaceSlug}/projects/${projectId}/work-items/${workItemId}/work-item-relations/`);
    }
    /**
     * Create one or more custom relations for a work item
     */
    async create(workspaceSlug, projectId, workItemId, createRelation) {
        return this.post(`/workspaces/${workspaceSlug}/projects/${projectId}/work-items/${workItemId}/work-item-relations/`, createRelation);
    }
    /**
     * Remove a custom relation between this work item and a target
     */
    async remove(workspaceSlug, projectId, workItemId, relatedWorkItemId) {
        return this.httpDelete(`/workspaces/${workspaceSlug}/projects/${projectId}/work-items/${workItemId}/work-item-relations/${relatedWorkItemId}/`);
    }
}
exports.CustomRelations = CustomRelations;
//# sourceMappingURL=CustomRelations.js.map