interface:
  display_name: Ambient Project Layer
  short_description: Quiet project capture and Plane synchronization
  default_prompt: Maintain a lightweight project view from meaningful work without interrupting the main task.
