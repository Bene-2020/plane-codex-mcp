{
  "mcp_servers": {
    "ambient-project": {
      "command": "node",
      "args": ["${PLUGIN_ROOT}/../apps/mcp/dist/index.js"],
      "env": {
        "AMBIENT_DB_PATH": "${AMBIENT_DB_PATH}",
        "PLANE_MODE": "${PLANE_MODE}",
        "PLANE_BASE_URL": "${PLANE_BASE_URL}",
        "PLANE_API_KEY": "${PLANE_API_KEY}"
      }
    }
  }
}
