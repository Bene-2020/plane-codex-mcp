# Shared UI components

The panel currently has no shared UI primitive directory. Buttons, cards, navigation rows, badges, form controls, and status controls are authored directly in apps/panel/src/main.tsx and styled in apps/panel/src/styles.css.

There are no reusable shared component files to embed. StandaloneConnect, Detail, and FailureList are page-local components and are indexed in pages.md.
