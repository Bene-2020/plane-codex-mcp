# 环境式项目管理层

本上下文描述一个非侵入式项目管理层：它观察用户在 Codex 中完成的工作，将有意义的项目信号转化为持久记录，并把这些记录投射到 Plane。

## 统一语言

**项目上下文（Project Context）**：
规范化 Codex 工作目录与用于组织其工作的 Plane 工作区、Plane 项目之间的持久关联。首次由用户自然选择，后续工作会话自动复用。
_避免使用_：项目绑定、Plane 连接

**工作会话（Work Session）**：
在某个项目上下文中进行的一次 Codex 对话。多个工作会话可以共同形成同一组项目记录。
_避免使用_：聊天、线程、Codex 任务

**工作回合（Work Turn）**：
工作会话中由一次用户提交和 Codex 随后的计划、工具执行及回复构成的完整工作单位。使用 Codex 提供的 `turn_id` 追踪。
_避免使用_：单条消息、一次 Hook

**来源事件（Source Event）**：
当前 Codex 在工作回合中识别出的有意义项目信号，例如任务、计划、Bug、决策、想法、风险、里程碑、进度或明确完成。它保留指向原始来源的引用。
_避免使用_：消息、通知

**项目事件批次（Project Event Batch）**：
当前 Codex 在一个工作回合结束前，通过 `record_project_events` 一次提交的零个或多个来源事件。有事件时每回合至多提交一次；没有事件时不提交空批次。
_避免使用_：Plane API 请求、语义模型输出

**项目记录（Project Record）**：
由一个或多个来源事件形成的、持久且用户可见的项目知识。项目记录可以是可执行的，也可以是信息性的。
_避免使用_：候选项、收件箱条目、抽取消息

**工作项（Work Item）**：
在 Plane 中表示为工作项的可执行项目记录，包括任务、Bug、风险、想法和里程碑。
_避免使用_：项目记录、Issue

**活动条目（Activity Entry）**：
附加到已有项目记录上的进度更新或证据，而不是一个独立的新工作项。
_避免使用_：任务、事件

**来源引用（Source Reference）**：
把项目记录或活动条目连接到其来源工作会话及来源摘要的溯源信息。
_避免使用_：反向链接、完整对话副本

**字段所有权（Field Ownership）**：
决定系统是否可以继续自动修改某个 Plane 字段的规则。用户手动修改后，该字段由用户接管；除用户明确授权外，系统不得覆盖。
_避免使用_：记录所有权、项目权限

**同步 Outbox（Sync Outbox）**：
高层 MCP 接受项目事件后，用于可靠异步投射到 Plane 的本地持久队列。它不是第二套项目数据库。
_避免使用_：本地真相源、项目缓存数据库

**项目面板（Project Panel）**：
用户查看、修正、组织和导航系统自动维护的项目记录时使用的可选界面。
_避免使用_：审批收件箱、确认队列
